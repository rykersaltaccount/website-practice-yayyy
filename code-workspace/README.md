# CodeVault Practice Workspace

Open this folder in VS Code, then use Connect workspace in CodeVault to sync files.

- `main.js` is the practice solution.
- `tests.js` contains focused tests.

The website checks this folder periodically for VS Code changes. Use Save to write website edits back to the folder.
